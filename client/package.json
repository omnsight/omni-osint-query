{
  "name": "client",
  "version": "1.0.0",
  "description": "This directory contains the generated TypeScript client for the API.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "generate": "openapi --input ../doc/openapi.json --output ./src --client axios",
    "test": "NODE_TLS_REJECT_UNAUTHORIZED=0 jest",
    "pretest": "npm run build",
    "prepare": "npm run build",
    "build": "tsc -p tsconfig.build.json"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "jest": "^30.2.0",
    "openapi-typescript-codegen": "^0.30.0",
    "ts-jest": "^29.4.6",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "axios": "^1.13.5",
    "form-data": "^4.0.5"
  }
}
